# Test environment: use the production-style relative API base so unit tests
# assert the same relative "/api/..." paths the app uses in production
# (the dev .env sets an absolute http://localhost:3001 base for the vite dev proxy,
# which must not leak into unit-test URL assertions).
VITE_API_BASE='/api'
